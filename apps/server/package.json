{
  "name": "server",
  "version": "1.0.0",
  "description": "server for the freelance project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"server test running\"",
    "dev": "rm -rf dist && yarn build && nodemon ./dist/src/index.js",
    "build": "rm -rf dist && tsc",
    "lint": "eslint src --fix",
    "start": "rm -rf dist && tsc && node ./dist/src/index.js"
  },
  "keywords": [
    "freelance"
  ],
  "author": "kuldeep",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.11",
    "config": "*",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "tsconfig": "*",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
